{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jcclub | Signup</title>
    <link rel="stylesheet" href="https://unpkg.com/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://unpkg.com/bs-brain@2.0.3/components/logins/login-6/assets/css/login-6.css">
    <style>
        body {
            background-size: cover;
        }
        .hjk {
            height: 10vh;
            background-color: aqua;
        }
    </style>
</head>
<body>
    <section class="m-5 pb-md-5 p-xl-5">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-md-9 col-lg-7 col-xl-6 col-xxl-6">
                    <div class="card border-0 shadow-lg rounded-4">
                        <div class="card-body p-3 p-md-4 p-xl-5">
                            <div class="row">
                                <div class="align align-items-end d-flex justify-content-end">
                            <a href="/login"><svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-house" viewBox="0 0 16 16">
                                <path d="M8.707 1.5a1 1 0 0 0-1.414 0L.646 8.146a.5.5 0 0 0 .708.708L2 8.207V13.5A1.5 1.5 0 0 0 3.5 15h9a1.5 1.5 0 0 0 1.5-1.5V8.207l.646.647a.5.5 0 0 0 .708-.708L13 5.793V2.5a.5.5 0 0 0-.5-.5h-1a.5.5 0 0 0-.5.5v1.293zM13 7.207V13.5a.5.5 0 0 1-.5.5h-9a.5.5 0 0 1-.5-.5V7.207l5-5z"/>
                            </svg>
                            </a>
                            </div>
                                <div class="col-12 align-content-center text-center">
                                    
                                    <div class="mb-5">
                                        <h3>Create Account</h3>
                                    </div>
                                </div>
                            </div>
                            <form action="signup" method="post" onsubmit="return validateForm()">
                                <div class="row gy-1 overflow-hidden">
                                    <div class="col-6">
                                        <div class="form-floating mb-3">
                                            <input type="text" class="form-control" name="firstName" id="firstName" placeholder="firstName" required>
                                            <label for="firstName" class="form-label">First Name</label>
                                        </div>
                                        <p id="errorFirstName" style="color: red;"></p>
                                    </div>
                                    <div class="col-6">
                                        <div class="form-floating mb-3">
                                            <input type="text" class="form-control" name="lastName" id="lastName" placeholder="lastName" required>
                                            <label for="lastName" class="form-label">Last Name</label>
                                        </div>
                                        <p id="errorLastName" style="color: red;"></p>
                                    </div>
                                    <div class="col-12">
                                        <div class="form-floating mb-3">
                                            <input type="email" class="form-control" name="mail" id="mail" placeholder="Optional">
                                            <label for="mail" class="form-label">Email</label>
                                        </div>
                                         {{#if mailExist}}
                                        <p style="color: red;">{{mailError}}</p>
                                        {{/if}}
                                        <p id="errorMail" style="color: red;"></p>
                                    </div>
                                    <div class="col-12">
                                        <div class="form-floating mb-3">
                                            <input type="tel" class="form-control" name="phoneNumber" id="phoneNumber" placeholder="Phone Number" required>
                                            <label for="phoneNumber" class="form-label">Phone Number</label>
                                        </div>
                                        <p id="errorPhoneNumber" style="color: red;"></p>
                                    </div>
                                    <div class="col-12">
                                        <div class="form-floating mb-3">
                                            <input type="password" class="form-control" name="password" id="password" placeholder="E" required>
                                            <label for="password" class="form-label">Password</label>
                                        </div>
                                        <p id="errorPassword" style="color: red;"></p>
                                    </div>
                                    <div class="row">
                                        <div class="col-12">
                                            <a class="text-decoration-non" href="/login"><p class=" link-secondary text-decoration-none">Already having account?</p></a>
                                        </div>
                                    </div>
                                    <div class="col-12">
                                        <div class="d-grid">
                                            <button class="btn bsb-btn-2xl btn-dark" type="submit">Signup</button>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script>
        function validateForm() {
            var firstName = document.getElementById("firstName").value;
            var lastName = document.getElementById("lastName").value;
            var mail = document.getElementById("mail").value;
            var phoneNumber = document.getElementById("phoneNumber").value;
            var password = document.getElementById("password").value;

            var isValid = true;

            // Regular expression for checking if the input consists only of characters
            var charRegex = /^[a-zA-Z]+$/;

            if (!charRegex.test(firstName)) {
                document.getElementById("errorFirstName").innerText = "First name must contain only characters.";
                isValid = false;
            } else {
                document.getElementById("errorFirstName").innerText = "";
            }

            if (!charRegex.test(lastName)) {
                document.getElementById("errorLastName").innerText = "Last name must contain only characters.";
                isValid = false;
            } else {
                document.getElementById("errorLastName").innerText = "";
            }

            // Email validation can be more complex, but here we'll just check if it's not empty
            if (mail === "") {
                document.getElementById("errorMail").innerText = "Please enter your email.";
                isValid = false;
            } else {
                document.getElementById("errorMail").innerText = "";
            }

            if (phoneNumber.length !== 10 || isNaN(phoneNumber)) {
                document.getElementById("errorPhoneNumber").innerText = "Phone number must be exactly 10 digits.";
                isValid = false;
            } else {
                document.getElementById("errorPhoneNumber").innerText = "";
            }

            if (password.length < 6) {
                document.getElementById("errorPassword").innerText = "Password must be at least 6 characters long.";
                isValid = false;
            } else {
                document.getElementById("errorPassword").innerText = "";
            }

            return isValid;
        }
    </script>
</body>
</html> --}}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jcclub | Signup</title>
    <link rel="stylesheet" href="https://unpkg.com/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://unpkg.com/bs-brain@2.0.3/components/logins/login-6/assets/css/login-6.css">
    <style>
        body {
            background-size: cover;
        }
        .hjk {
            height: 10vh;
            background-color: aqua;
        }
    </style>
</head>
<body>
    <section class="m-5 pb-md-5 p-xl-5">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-md-9 col-lg-7 col-xl-6 col-xxl-6">
                    <div class="card border-0 shadow-lg rounded-4">
                        <div class="card-body p-3 p-md-4 p-xl-5">
                            <div class="row">
                                <div class="align align-items-end d-flex justify-content-end">
                                    <a href="/login"><svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" class="bi bi-house" viewBox="0 0 16 16">
                                        <path d="M8.707 1.5a1 1 0 0 0-1.414 0L.646 8.146a.5.5 0 0 0 .708.708L2 8.207V13.5A1.5 1.5 0 0 0 3.5 15h9a1.5 1.5 0 0 0 1.5-1.5V8.207l.646.647a.5.5 0 0 0 .708-.708L13 5.793V2.5a.5.5 0 0 0-.5-.5h-1a.5.5 0 0 0-.5.5v1.293zM13 7.207V13.5a.5.5 0 0 1-.5.5h-9a.5.5 0 0 1-.5-.5V7.207l5-5z"/>
                                    </svg>
                                    </a>
                                </div>
                                <div class="col-12 align-content-center text-center">
                                    <div class="mb-5">
                                        <h3>Create Account</h3>
                                    </div>
                                </div>
                            </div>
                            <form action="signup" method="post" >
                                <div class="row gy-1 overflow-hidden">
                                    <div class="col-6">
                                        <div class="form-floating mb-3">
                                            <input type="text" class="form-control" name="firstName" id="firstName" placeholder="firstName" required>
                                            <label for="firstName" class="form-label">First Name</label>
                                        </div>
                                        <p id="errorFirstName" style="color: red;"></p>
                                    </div>
                                    <div class="col-6">
                                        <div class="form-floating mb-3">
                                            <input type="text" class="form-control" name="lastName" id="lastName" placeholder="lastName" required>
                                            <label for="lastName" class="form-label">Last Name</label>
                                        </div>
                                        <p id="errorLastName" style="color: red;"></p>
                                    </div>
                                    <div class="col-12">
                                        <div class="form-floating mb-3">
                                            <input type="email" class="form-control" name="mail" id="mail" placeholder="Optional">
                                            <label for="mail" class="form-label">Email</label>
                                        </div>
                                        {{#if mailExist}}
                                        <p style="color: red;">{{mailError}}</p>
                                        {{/if}}
                                        <p id="errorMail" style="color: red;"></p>
                                    </div>
                                    <div class="col-12">
                                        <div class="form-floating mb-3">
                                            <input type="tel" class="form-control" name="phoneNumber" id="phoneNumber" placeholder="Phone Number" required>
                                            <label for="phoneNumber" class="form-label">Phone Number</label>
                                        </div>
                                        <p id="errorPhoneNumber" style="color: red;"></p>
                                    </div>
                                    <div class="col-12">
                                        <div class="form-floating mb-3">
                                            <input type="password" class="form-control" name="password" id="password" placeholder="E" required>
                                            <label for="password" class="form-label">Password</label>
                                        </div>
                                        <p id="errorPassword" style="color: red;"></p>
                                    </div>
                                    <div class="row">
                                        <div class="col-12">
                                            <a class="text-decoration-non" href="/login"><p class=" link-secondary text-decoration-none">Already having account?</p></a>
                                        </div>
                                    </div>
                                    <div class="col-12">
                                        <div class="d-grid">
                                            <button id="signupBtn" class="btn bsb-btn-2xl btn-dark" type="submit" >Signup</button>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        $(document).ready(function() {
            $('#firstName, #lastName, #mail, #phoneNumber, #password').on('input', function() {
                validateInputs();
            });

            function validateInputs() {
                var isValid = true;
                $('input').each(function() {
                    var errorId = '#error' + $(this).attr('id').charAt(0).toUpperCase() + $(this).attr('id').slice(1);
                    if ($(this).val() === '') {
                        $(errorId).text('This field is required.');
                        isValid = false;
                    } else {
                        $(errorId).text('');
                    }
                });

                // Validate first name and last name format
                var firstName = $('#firstName').val();
                var lastName = $('#lastName').val();
                var charRegex = /^[a-zA-Z]*$/; // Only characters allowed
                if (!charRegex.test(firstName)) {
                    $('#errorFirstName').text('First name must contain only characters.');
                    isValid = false;
                } else {
                    $('#errorFirstName').text('');
                }
                if (!charRegex.test(lastName)) {
                    $('#errorLastName').text('Last name must contain only characters.');
                    isValid = false;
                } else {
                    $('#errorLastName').text('');
                }

                // Validate email format
                var email = $('#mail').val();
                if (!validateEmail(email)) {
                    $('#errorMail').text('Please enter a valid email address.');
                    isValid = false;
                } else {
                    $('#errorMail').text('');
                }

                // Validate phone number format
                var phoneNumber = $('#phoneNumber').val();
                if (!validatePhoneNumber(phoneNumber)) {
                    $('#errorPhoneNumber').text('Please enter a valid phone number.');
                    isValid = false;
                } else {
                    $('#errorPhoneNumber').text('');
                }

                // Validate password format
                var password = $('#password').val();
                if (!validatePassword(password)) {
                    $('#errorPassword').text('Password must contain at least one uppercase letter and one digit.');
                    isValid = false;
                } else {
                    $('#errorPassword').text('');
                }

            }

            // Email validation function
            function validateEmail(email) {
                var regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                return regex.test(email);
            }

            // Phone number validation function
            function validatePhoneNumber(phoneNumber) {
                var regex = /^\d{10}$/; // 10 digits exactly
                return regex.test(phoneNumber);
            }

            // Password validation function
            function validatePassword(password) {
                var upperCaseRegex = /[A-Z]/;
                var digitRegex = /\d/;
                return upperCaseRegex.test(password) && digitRegex.test(password);
            }
        });
    </script>
</body>
</html>
